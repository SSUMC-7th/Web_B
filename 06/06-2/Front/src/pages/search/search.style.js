import styled from "styled-components"

const SearchText = styled.h1`
    color : white;
`;

const SearchContainer = styled.div`
    display : flex;
    justify-content : center;
`

export {SearchText, SearchContainer}